# Environment configuration for homelab-vm-provisioner-client
# Copy this file to .env and customize as needed
# Parent workspace .env variables are loaded first, then these override them

# Build-time configuration
# Add any client-specific build variables here
# Currently no client-specific variables required
